vamos fazer a interface do projeto, vai ser vanilla mesmo.


tera no header, um campo de input de texto, para digitar um nome,
e terá em baixo uma uma lista de 20 nomes aleatorios para exemplo, aonde apenas clicar encima ele preenche o campo